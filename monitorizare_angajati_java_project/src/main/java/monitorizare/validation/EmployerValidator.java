package monitorizare.validation;

import monitorizare.domain.Employer;

public class EmployerValidator implements Validator<Employer> {
    @Override
    public void validate(Employer entity) throws RuntimeException {

    }
}
